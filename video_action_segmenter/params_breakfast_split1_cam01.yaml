# Breakfast split1 cam01 — training energy export config
checkpoint_path: "/home/johnny/action_ws/checkpoints/motion_tokenizer/epochs10_breakfast_split1_cam01_m10/best.pt"
decoder_window_size: 320

device: "cuda"
amp: true
visualize: false
show_overlay: false
window_title: "MotionTokenizer Stream"
display_fps: 10

input:
  type: "folder"
  dir: "/home/johnny/action_ws/online_datasets/breakfast/breakfast/Videos_train.split1_cam01"
  video_exts: [".avi"]
  recursive: true
  batch:
    size: 1
  eof_timeout_seconds: 5

target_fps: 10
resize_shorter: 320
stride: 4

# Optional: stream codes (disabled by default)
jsonl_output: false
jsonl_path: "/home/johnny/action_ws/output/breakfast/energy_split1_cam01/stream_codes.jsonl"

energy:
  enable: true
  source: "quantized"            # prequant | quantized | velocity
  mode: "token_diff_l2_mean"     # l2_mean | token_diff_l2_mean
  visualize: false
  jsonl_output: true
  jsonl_path: "/home/johnny/action_ws/output/breakfast/energy_split1_cam01"
  smoothing:
    enable: false

segmentation:
  enable: false

